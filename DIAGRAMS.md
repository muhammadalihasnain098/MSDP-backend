# System Architecture Diagrams

Visual representation of the MSDP system architecture.

## 🏗️ High-Level System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
│                                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │  Admin   │  │  Health  │  │   Lab    │  │Pharmacist│        │
│  │Dashboard │  │ Official │  │   Tech   │  │   POS    │        │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘        │
│        │             │              │              │             │
└────────┼─────────────┼──────────────┼──────────────┼─────────────┘
         │             │              │              │
         │             │              │              │
         └─────────────┴──────────────┴──────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      NEXT.JS FRONTEND                            │
│                     (React Application)                          │
│                      Port: 3000                                  │
│                                                                   │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐                │
│  │   Pages    │  │ Components │  │  API Layer │                │
│  │  Routing   │  │    UI      │  │ (lib/api)  │                │
│  └────────────┘  └────────────┘  └──────┬─────┘                │
│                                          │                       │
└──────────────────────────────────────────┼───────────────────────┘
                                           │
                                           │ HTTP/REST
                                           │ JWT Auth
                                           │
                                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                   DJANGO REST FRAMEWORK                          │
│                       (Backend API)                              │
│                      Port: 8000                                  │
│                                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │  Users   │  │ Datasets │  │Forecast  │  │ Reports  │        │
│  │   App    │  │   App    │  │   App    │  │   App    │        │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘        │
│        │             │              │              │             │
│        └─────────────┴──────────────┴──────────────┘             │
│                            │                                     │
│  ┌─────────────────────────┴─────────────────────┐              │
│  │         Django ORM (Database Layer)           │              │
│  └───────────────────────┬───────────────────────┘              │
│                          │                                       │
└──────────────────────────┼───────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  PostgreSQL  │  │    Redis     │  │   Storage    │
│   Database   │  │ (Task Queue) │  │ (ML Models)  │
│              │  │              │  │              │
│  • Users     │  │ • Celery     │  │ • .joblib    │
│  • Datasets  │  │   Broker     │  │   files      │
│  • Models    │  │ • Cache      │  │ • Uploads    │
│  • Forecasts │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
```

## 🔄 Request Flow

### 1. User Login Flow
```
User enters credentials
         │
         ▼
Next.js Login Page
         │
         ▼
POST /api/users/login/
         │
         ▼
Django LoginView
         │
         ▼
Authenticate User
         │
         ▼
Generate JWT Tokens
         │
         ▼
Return {user, tokens}
         │
         ▼
Store in localStorage
         │
         ▼
Redirect to Dashboard
```

### 2. File Upload Flow
```
User selects file
         │
         ▼
Frontend FormData
         │
         ▼
POST /api/datasets/
         │
         ▼
Django DatasetViewSet
         │
         ▼
Save file to media/
         │
         ▼
Create Dataset record
         │
         ▼
Queue Celery task
         │
         ├─────────────┐
         │             ▼
         │      Celery Worker
         │             │
         │             ▼
         │      Validate File
         │             │
         │             ▼
         │      Update Status
         │
         ▼
Return 201 Created
```

### 3. ML Training Flow
```
Admin triggers training
         │
         ▼
POST /api/forecasting/models/
         │
         ▼
Create ForecastModel
         │
         ▼
Queue training task
         │
         ├─────────────┐
         │             ▼
         │      Celery Worker
         │             │
         │             ▼
         │      Load Dataset
         │             │
         │             ▼
         │      Train Model
         │             │
         │             ▼
         │    Save .joblib file
         │             │
         │             ▼
         │    Update Model Status
         │
         ▼
Return model info
```

## 📊 Database Schema

```
┌──────────────┐
│    User      │
├──────────────┤
│ id           │◄───┐
│ username     │    │
│ email        │    │
│ role         │    │
│ password     │    │
└──────────────┘    │
                    │
                    │ uploaded_by
                    │
┌──────────────┐    │
│   Dataset    │    │
├──────────────┤    │
│ id           │    │
│ name         │────┘
│ file         │
│ status       │◄───┐
│ uploaded_by  │    │
└──────────────┘    │
                    │ dataset
                    │
┌──────────────┐    │
│ForecastModel │    │
├──────────────┤    │
│ id           │    │
│ name         │────┘
│ version      │
│ model_file   │◄───┐
│ dataset_id   │    │
│ status       │    │
└──────────────┘    │
                    │ model
                    │
┌──────────────┐    │
│  Forecast    │    │
├──────────────┤    │
│ id           │    │
│ disease      │────┘
│ region       │
│ predicted    │
│ model_id     │
└──────────────┘
```

## 🔐 Authentication Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                       │
└─────────────────────────────────────────────────────────────┘

Step 1: Login
─────────────
Client                          Server
  │                               │
  │  POST /api/users/login/       │
  │  {username, password}         │
  ├──────────────────────────────►│
  │                               │
  │                               ├─► Validate credentials
  │                               │
  │                               ├─► Generate JWT tokens
  │                               │   • Access Token (60 min)
  │                               │   • Refresh Token (24 hr)
  │                               │
  │  {user, tokens}               │
  │◄──────────────────────────────┤
  │                               │
  ├─► Store in localStorage      │
  │                               │

Step 2: Protected Request
──────────────────────────
Client                          Server
  │                               │
  │  GET /api/datasets/           │
  │  Header:                      │
  │  Authorization:               │
  │  Bearer eyJ0eXAi...           │
  ├──────────────────────────────►│
  │                               │
  │                               ├─► Verify JWT signature
  │                               │
  │                               ├─► Check expiration
  │                               │
  │                               ├─► Extract user_id
  │                               │
  │                               ├─► Load user from DB
  │                               │
  │                               ├─► Check permissions
  │                               │
  │  {data}                       │
  │◄──────────────────────────────┤
  │                               │

Step 3: Token Refresh
──────────────────────
Client                          Server
  │                               │
  │  POST /api/users/refresh/     │
  │  {refresh: "..."}             │
  ├──────────────────────────────►│
  │                               │
  │                               ├─► Verify refresh token
  │                               │
  │                               ├─► Generate new access
  │                               │
  │  {access: "..."}              │
  │◄──────────────────────────────┤
  │                               │
```

## 🔄 Celery Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    CELERY TASK SYSTEM                       │
└────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Django     │
│   API View   │
└──────┬───────┘
       │
       │ 1. Queue Task
       │
       ▼
┌──────────────┐     2. Store Task     ┌──────────────┐
│    Redis     │◄─────────────────────►│   Celery     │
│   (Broker)   │     3. Get Next Task  │   Worker     │
└──────────────┘                        └──────┬───────┘
                                               │
                                               │ 4. Execute
                                               │
                                               ▼
                                        ┌──────────────┐
                                        │   Process    │
                                        │   • Train    │
                                        │   • Validate │
                                        │   • Generate │
                                        └──────┬───────┘
                                               │
                                               │ 5. Update DB
                                               │
                                               ▼
                                        ┌──────────────┐
                                        │  PostgreSQL  │
                                        │   Database   │
                                        └──────────────┘

Celery Beat (Scheduler)
───────────────────────
┌──────────────┐
│ Celery Beat  │
│  (Cron Jobs) │
└──────┬───────┘
       │
       │ Scheduled Task
       │ (e.g., daily forecast)
       │
       ▼
┌──────────────┐
│    Redis     │
│   (Broker)   │
└──────────────┘
```

## 🌐 Deployment Architecture

```
┌────────────────────────────────────────────────────────────┐
│                   PRODUCTION DEPLOYMENT                     │
└────────────────────────────────────────────────────────────┘

Internet
   │
   │
   ├────────────────────┬────────────────────┐
   │                    │                    │
   ▼                    ▼                    ▼
┌─────────┐      ┌─────────┐        ┌─────────┐
│ Vercel  │      │ Render  │        │ Render  │
│         │      │  Web    │        │ Worker  │
│ Next.js │      │ Django  │        │ Celery  │
│Frontend │      │  API    │        │ Worker  │
└────┬────┘      └────┬────┘        └────┬────┘
     │                │                   │
     │ API Calls      │                   │
     └────────────────┤                   │
                      │                   │
                      │ DB Queries        │
                      │                   │
                      ▼                   │
               ┌─────────────┐            │
               │    Neon     │            │
               │ PostgreSQL  │            │
               │  Database   │            │
               └─────────────┘            │
                      │                   │
                      │                   │
                      │ Task Queue        │
                      ├───────────────────┘
                      │
                      ▼
               ┌─────────────┐
               │   Upstash   │
               │    Redis    │
               │             │
               └─────────────┘

Environment Variables Flow:
──────────────────────────
Vercel:
  NEXT_PUBLIC_API_URL → Points to Render backend

Render (Web Service):
  DATABASE_URL → Points to Neon
  REDIS_URL → Points to Upstash
  SECRET_KEY → Generated
  ALLOWED_HOSTS → Render domain
  FRONTEND_URL → Vercel domain

Render (Worker):
  DATABASE_URL → Same as web
  REDIS_URL → Same as web
```

## 📱 Application Flow

```
┌────────────────────────────────────────────────────────────┐
│                   USER INTERACTION FLOW                     │
└────────────────────────────────────────────────────────────┘

Admin User Journey:
──────────────────
Login → Admin Dashboard → User Management
                        → Model Training
                        → System Monitoring
                        → Data Upload

Health Official Journey:
────────────────────────
Login → Dashboard → View Forecasts
                  → Risk Heatmap
                  → Resource Estimator
                  → Generate Reports

Lab Technician Journey:
───────────────────────
Login → Lab Dashboard → Enter Test Results
                      → Upload Datasets
                      → View Entry History

Pharmacist Journey:
──────────────────
Login → POS Interface → Enter Sales Data
                      → Track Inventory
                      → View History
```

## 🔄 Data Flow

```
┌────────────────────────────────────────────────────────────┐
│                      DATA PIPELINE                          │
└────────────────────────────────────────────────────────────┘

1. Data Collection
   ───────────────
   Lab Results ──┐
   Pharmacy Sales┼─► Upload → Validation → Storage
   Manual Entry ─┘

2. Data Processing
   ────────────────
   Raw Data → Clean → Transform → Feature Engineering

3. Model Training
   ───────────────
   Prepared Data → Train ML Model → Validate → Save

4. Forecasting
   ────────────
   New Data → Load Model → Predict → Store Results

5. Reporting
   ──────────
   Forecasts → Aggregate → Format → Export (Excel/PDF)

6. Visualization
   ──────────────
   Results → Charts → Heatmaps → Dashboard
```

---

These diagrams help visualize the complete system architecture. Use them as reference while reading the documentation!
